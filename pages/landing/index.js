import { Box, Typography } from "@mui/material";

export default function Page(){
    return <Box>
        <Typography>Upload photo</Typography>
        <Typography>Report and manage</Typography>
        <Typography>Active cases</Typography>
    </Box>
}